import React, { Fragment } from "react";
import PropTypes from "prop-types";
const Time = ({ date }) => (
  <Fragment>
  </Fragment>
);

Time.propTypes = {
  date: PropTypes.string
};

export default Time;